# Shared secret sent as X-API-Key on every request to the backend (src/config.ts) once
# the backend's own MAT_API_KEY (mat-ai-os/.env) is set. Must match it exactly. Leave
# blank if the backend has no MAT_API_KEY configured (local-only use).
VITE_MAT_API_KEY=
